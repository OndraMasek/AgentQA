// Built-in agent — wired into the registry. The prompt lives in prompt.md;
// this file exists so the directory is discoverable and so a future custom
// agent author can copy this layout. Materialisation lives in
// `src/agents/registry.ts`.

export const NAME = "functional" as const;
